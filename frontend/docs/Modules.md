1. Module Declaration
   
        define modulename co.lang.signature={
        }
   


3. Modules Here works similar to ML whatever above we declared is just type  
   unlike classes or structs we cannnot initiate and they must be assigned with module value at actual variable declaration time.

4. Create Module values for given type

   a.  implementing

        @co.dap.modulesig(modulename)           )
        impl mymod co.lang.module->(matches=modulename) = {


       }

   b. declaring

       define mm modulename= mymod; //initiazation is must
   
    
